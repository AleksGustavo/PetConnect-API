/**
 * Módulo <b>appointment</b> do monólito modular do PetConnect.
 *
 * <p>Estrutura prevista (criada nas fases seguintes):
 * <ul>
 *   <li>{@code domain} — entidade + regras puras</li>
 *   <li>{@code application} — casos de uso / serviços</li>
 *   <li>{@code infrastructure} — repositório MongoDB</li>
 *   <li>{@code web} — controller REST + DTOs sob {@code /api/v1}</li>
 * </ul>
 * Ainda vazio na FASE 1 (esqueleto).
 */
package com.petconnect.api.appointment;
