package com.petconnect.api;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Sobe o contexto inteiro (com MongoDB embarcado) para garantir que o esqueleto está consistente. */
@SpringBootTest
class PetConnectApiApplicationTests {

    @Test
    void contextLoads() {
    }
}
